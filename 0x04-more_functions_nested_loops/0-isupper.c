#include "main.h"

/**
  * _isupper - checks if a letter is upper
  * @a: number
  *
  * Return: 1 or 0
  */
int _isupper(int a)
{
	if (a >= 'A' && a <= 'Z')
	{
		return (1);
	}
	else
	{
	return (0);
	}
}
